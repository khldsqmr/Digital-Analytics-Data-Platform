1. File dedup grain: my query picks the latest file per individual campaign; yours picks it at the LOB level (all campaigns combined). If different campaigns got updated in different weekly files, that can miss some updates.
2. Boundary weeks (quarter transitions): my query splits spend proportionally by calendar days across the two quarters; yours doesn't split at all, so those specific weeks can look off.
3. A couple of filter differences — 'Unallocated' channel and 'Micro' message types are handled oppositely between the two.
