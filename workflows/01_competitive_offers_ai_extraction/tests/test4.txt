SELECT UPPER(TRIM(LOB_Supported)) AS raw_lob_value, COUNT(*) AS row_count, SUM(Spend) AS total_spend
FROM `prj-dbi-prd-1.ds_dbi_marketing.ma_mfc_raw`
GROUP BY raw_lob_value
ORDER BY row_count DESC;
