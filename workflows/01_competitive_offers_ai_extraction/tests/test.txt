//c.tms.MFC Forecasts
SUM(IF [Metric Name] = 'mfcSpendForecast' THEN [Metric Value] END)

//c.tms.MFC Forecasts Total
SUM(
  IF [Metric Name] = 'mfcSpendForecast'
  AND [Qgp Date] <= [c.tms.MFC Max Actual Date]
  THEN [Metric Value]
  END
)

//c.tms.MFC Actuals
SUM(IF [Metric Name] = 'mfcSpendActual' THEN [Metric Value] END)

//c.tms.MFC Actuals Total
SUM(IF [Metric Name] = 'mfcSpendActual' THEN [Metric Value] END)
