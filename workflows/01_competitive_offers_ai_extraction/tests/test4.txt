WITH ranked AS (
  SELECT *,
    ROW_NUMBER() OVER (
      PARTITION BY Agency, Message, Market, Platform, Tactic, Week_Beginning_Monday, QGP_Week
      ORDER BY FileLoad_Date DESC, FileModified_TimeStamp DESC
    ) AS rn
  FROM prdrzranalytics.lab42.raw_media_flowchart
)
SELECT
  QGP_Week,
  Week_Beginning_Monday,
  Quarter,
  SUM(Spend_Actual)   AS spend_actual,
  SUM(Spend_Forecast) AS spend_forecast
FROM ranked
WHERE rn = 1
GROUP BY QGP_Week, Week_Beginning_Monday, Quarter
ORDER BY QGP_Week;
