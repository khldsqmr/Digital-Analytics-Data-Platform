from vertexai.generative_models import Tool, grounding

test_model = GenerativeModel(GEMINI_MODEL)
search_tool = Tool.from_google_search_retrieval(grounding.GoogleSearchRetrieval())
test_response = test_model.generate_content(
    "Is 'Boost Infinite' a real wireless carrier or MVNO brand? Answer in one sentence.",
    tools=[search_tool],
)
print(test_response.text)
print("\n--- grounding metadata (should show search queries + sources if it worked) ---")
print(test_response.candidates[0].grounding_metadata)
