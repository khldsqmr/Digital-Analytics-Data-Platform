IF [Data Source] = 'ADOBE' AND [Metric Name] = 'upvTotalAdobe'
THEN [Metric Value]
END

IF [Data Source] = 'UPV_FORECAST' AND [Metric Name] = 'upvForecast'
THEN [Metric Value]
END

(SUM([Actual UPV]) - SUM([Forecast UPV])) / ABS(SUM([Forecast UPV]))

IF SUM([Forecast UPV]) = 0 OR ISNULL(SUM([Forecast UPV]))
THEN 'No Forecast'
ELSEIF SUM([Actual UPV]) = 0 OR ISNULL(SUM([Actual UPV]))
THEN 'No Actuals Yet'
ELSE STR(ROUND((SUM([Actual UPV]) - SUM([Forecast UPV])) / ABS(SUM([Forecast UPV])) * 100, 1)) + '%'
END

qgp_date on Columns
channel_group on Filters (or Rows if you want a breakdown)
Marks: dual axis with SUM([Actual UPV]) and SUM([Forecast UPV]) as bars/lines
[Actual vs Forecast %] as a second axis or label
